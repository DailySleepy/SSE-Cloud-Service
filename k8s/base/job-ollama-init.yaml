# NOTE: cb-ollama-chat-init Job 已移除
# 模型拉取与预热已合并到 cb-ollama-chat StatefulSet 的启动脚本中
# startupProbe 负责保护整个加载过程

apiVersion: batch/v1
kind: Job
metadata:
  name: cb-ollama-embed-init
spec:
  backoffLimit: 4
  template:
    spec:
      containers:
        - name: cb-ollama-embed-init
          image: ollama/ollama:0.5.4
          imagePullPolicy: IfNotPresent
          env:
            - name: OLLAMA_HOST
              value: "http://cb-ollama-embed:11434"
          command: ["/bin/sh", "-c"]
          args:
            - |
              echo 'Wait for Ollama embed server...'
              while ! ollama list > /dev/null 2>&1; do
                sleep 2
              done
              echo 'Pulling bge-m3 (embedding model)...'
              until ollama pull bge-m3; do
                echo 'Pulling bge-m3 failed, retrying in 5 seconds...'
                sleep 5
              done
              echo 'Embed model pulled successfully!'
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
      restartPolicy: Never
