FROM python:3.12-slim

COPY --from=ghcr.io/astral-sh/uv:0.4.15 /uv /bin/uv

COPY . /app
WORKDIR /app

RUN uv sync --frozen || uv sync

ENV PATH="/app/.venv/bin:$PATH"

EXPOSE 8000

CMD ["uvicorn", "services.ingestor.main:app", "--host", "0.0.0.0", "--port", "8000"]
